/**
 * Focus — Content Script (Blocker)
 *
 * Stage 2 placeholder. Will implement:
 *   - MutationObserver for SPA support
 *   - Shadow DOM overlay for blocked sites
 *   - Tailwind-styled "Focus Mode" UI
 *
 * Injected at document_start for instant blocking.
 */

// Placeholder — full implementation in Stage 2
(function () {
  'use strict';
  // Content script ready — blocker logic will be added here
})();
